<%- include('../includes/head') %>
   <%- include('../includes/header') %>
       
   <% if (data != null) {%>
                <div class="center">
                  <h1>Images are sorted by size.</h1>
                  <%for(var i=0;i< data.length;i++){%>
                        <h3>
                           Image Size:  <%=data[i].fileSizeBytes%>
                        </h3>
                    <div class="item-center">
                        <img class="main_img" src="<%=data[i].url%>" alt="rasm bo'lishi kerak edi.">
                    </div>
                    <% }%>
            <% } else { %>
                <h1>No image found</h1>
                <% } %>
            </div>
</body>
</html>